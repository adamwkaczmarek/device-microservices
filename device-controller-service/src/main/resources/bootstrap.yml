spring:
  application:
    name: device-controller-service
  profiles:
    active:
      dev
  cloud:
    config:
      enabled: true
      label: devices-ms-config
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        device-data-update-in:
          destination: device-data-update
          contentType: application/json

security.jwt.client-id: springcloudtemp
security.jwt.client-secret: sct_secret
security.tech-user-name: device
security.tech-password: test

redis.server: localhost
redis.port: 6379

