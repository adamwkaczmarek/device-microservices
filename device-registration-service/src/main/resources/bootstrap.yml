spring:
  application:
    name: device-registration-service
  profiles:
    active:
      dev
  cloud:
    config:
      enabled: false
      label: devices-ms-config

queue:
    registration: RegisterDevicesQueue
    deviceHealth: DeviceHealthQueue


