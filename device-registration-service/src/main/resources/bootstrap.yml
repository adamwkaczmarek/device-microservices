spring:
  application:
    name: device-registration-service
  profiles:
    active:
      aws-dev
  cloud:
    config:
      enabled: true
      label: devices-ms-config

cloud:
    aws:
      credentials:
        accessKey: <ACCESSKEY>
        secretKey: <SECRETKEY>
      region:
        static: eu-central-1


queue:
    registration: RegisterDevicesQueue


